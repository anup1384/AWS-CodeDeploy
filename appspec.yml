version: 0.0
os: linux
files:
   - source: target/fareye_routing_engine-0.0.1-SNAPSHOT.war
     destination: /home/ubuntu/deploy/
hooks:
  BeforeInstall:
    - location: scripts/beforeinstall.sh
      timeout: 120
      runas: root
  AfterInstall:
    - location: scripts/afterInstall.sh
      timeout: 60
      runas: root

